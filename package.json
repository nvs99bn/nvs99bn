{
  "name": "the-sunny-profile",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {"build": "esbuild src/game.js --bundle --minify --format=iife --target=es2020 --outfile=game.js", "test": "playwright test"},
  "dependencies": {"three": "0.186.0"},
  "devDependencies": {"esbuild": "0.28.2", "@playwright/test": "1.63.0"}
}
